# Certificados locais

Coloque aqui, apenas localmente, o certificado CA do Aiven quando a conexao MySQL exigir SSL.

Exemplo de arquivo local:

```text
certs/aiven-ca.pem
```

Nao versione certificados reais, chaves privadas ou arquivos como `.pem`, `.key`, `.p12` e `.jks`.
